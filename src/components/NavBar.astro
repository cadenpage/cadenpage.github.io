<header class="site-header">
	<div class="container header-inner">
		<a class="identity" href="/" aria-label="Caden Page home">
			<span class="identity-name">Caden Page</span>
			<span class="identity-focus">Robotics &amp; Mechanical Engineering</span>
		</a>

		<nav aria-label="Main navigation">
			<a href="#home">Home</a>
			<a href="#projects">Projects</a>
			<a href="#research">Research</a>
			<a href="#experience">Experience</a>
			<a href="#cv">CV</a>
			<a href="#contact">Contact</a>
		</nav>
	</div>
</header>

<style>
	.site-header {
		background: rgba(247, 249, 250, 0.92);
		border-bottom: 1px solid var(--color-line);
		position: sticky;
		top: 0;
		z-index: 10;
		backdrop-filter: blur(12px);
	}

	.header-inner {
		align-items: center;
		display: flex;
		gap: 2rem;
		justify-content: space-between;
		min-height: 76px;
	}

	.identity {
		display: grid;
		gap: 0.2rem;
		text-decoration: none;
	}

	.identity-name {
		color: var(--color-ink);
		font-size: 1.07rem;
		font-weight: 650;
		letter-spacing: -0.02em;
	}

	.identity-focus {
		color: var(--color-muted);
		font-size: 0.78rem;
	}

	nav {
		align-items: center;
		display: flex;
		flex-wrap: wrap;
		gap: 1.35rem;
	}

	nav a {
		color: var(--color-muted);
		font-size: 0.92rem;
		font-weight: 500;
		text-decoration: none;
		transition: color 150ms ease;
	}

	nav a:hover,
	nav a:focus-visible {
		color: var(--color-accent);
	}

	@media (max-width: 760px) {
		.header-inner {
			align-items: flex-start;
			flex-direction: column;
			gap: 1rem;
			padding-bottom: 1.1rem;
			padding-top: 1.1rem;
		}

		nav {
			gap: 0.8rem 1.2rem;
		}
	}
</style>
